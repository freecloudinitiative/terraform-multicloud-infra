name: "Terraform Apply"

on:
  push:
    branches:
      - main
    paths:
      - "**.tf"
  workflow_dispatch:

jobs:
  apply:
    uses: freecloudinitiative/.github/.github/workflows/terraform-apply.yml@2816605944e9c3355e401d527f7f725b05b71ce5
    with:
      directory: '.'
    secrets: inherit
